{
  "name": "@blocks/list-grid",
  "version": "1.0.0",
  "description": "La lista de cuadrículas se puede realizar configurando la propiedad `grid` de` List`, y `column` puede establecer el número de columnas que se desea mostrar.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "start-c": "node src/index.js -c",
    "dev": "umi dev",
    "test": "jest --config .jest.js --no-cache"
  },
  "license": "MIT",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/machinaai"
  },
  "dependencies": { "@ant-design/icons": "^4.0.2", "antd": "^4.0.0" }
}
