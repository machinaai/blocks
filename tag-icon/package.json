{
  "name": "@blocks/tag-icon",
  "version": "1.0.0",
  "description": "Cuando necesite incrustar `Icon` en` Tag`, puede establecer el atributo `icon`, o usar el componente` Icon` directamente en `Tag`. \n\nSi desea controlar la ubicación específica de `Icon`, solo puede usar el componente` Icon` directamente, no el atributo `icon`.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "start-c": "node src/index.js -c",
    "dev": "umi dev",
    "test": "jest --config .jest.js --no-cache"
  },
  "license": "MIT",
  "publishConfig": { "registry": "https://npm.pkg.github.com/machinaai" },
  "dependencies": { "@ant-design/icons": "^4.0.2", "antd": "^4.0.0" }
}
