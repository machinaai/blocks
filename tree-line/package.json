{
  "name": "@blocks/tree-line",
  "version": "1.0.0",
  "description": "Los árboles con líneas de conexión entre nodos a menudo se usan para mostrar estructuras de directorios de archivos. Use `showLine` para abrir, puede usar` switcherIcon` para modificar el icono predeterminado.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "start-c": "node src/index.js -c",
    "dev": "umi dev",
    "test": "jest --config .jest.js --no-cache"
  },
  "license": "MIT",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/machinaai"
  },
  "dependencies": { "@ant-design/icons": "^4.0.2", "antd": "^4.0.0" }
}
