{
  "name": "@blocks/grid-offset",
  "version": "1.0.0",
  "description": "Desplazamiento de columna. \n\nUtilice `offset` para desplazar la columna a la derecha. Por ejemplo, `offset = {4}` desplaza el elemento a la derecha por el ancho de 4 columnas.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "start-c": "node src/index.js -c",
    "dev": "umi dev",
    "test": "jest --config .jest.js --no-cache"
  },
  "license": "MIT",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com/machinaai"
  },
  "dependencies": { "@ant-design/icons": "^4.0.2", "antd": "^4.0.0" }
}
